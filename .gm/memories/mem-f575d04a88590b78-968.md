---
key: mem-f575d04a88590b78-968
ns: default
created: 1783349991525
updated: 1783349991525
---

## Resolved mutable: mut-1783349662073

Confirmed root cause: multiple concurrent gm sessions dispatching the browser verb against the same shared playwriter relay/executor for this repo cross-deliver responses (session_id 3/4 reused across sessions, response bodies not matching the dispatched script). Not a gmsniff code defect -- an artifact of concurrent-session browser-verb contention on a shared exec-spool. My own toast-stacking-and-lifecycle-audit row's browser witnesses (browser-7, browser-8) were verified self-consistent by checking result.result against the exact script dispatched each time (toastCount/hostFound fields matched what each script computed) before treating them as valid witness, so this row's verification is unaffected. Workaround already demonstrated by prior session: verify via exec_js against the real running server + direct source read when browser-verb cross-talk is suspected, rather than retrying the contended executor blindly.
