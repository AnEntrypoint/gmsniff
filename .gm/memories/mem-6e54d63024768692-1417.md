---
key: mem-6e54d63024768692-1417
ns: default
created: 1783350063423
updated: 1783350063423
---

## Resolved mutable: mut-1783350037113

gui/panels.js:726-737 edited: added `if (prd.error || mutables.error) return Empty('Failed to load lifecycle state: ' + (prd.error || mutables.error));` immediately after Promise.all([api('/api/prd'...), api('/api/mutables'...)]). Real-execution witness via exec_js against the live gui server (node src/cli.js gui --port 8734): (1) exec_js-2.json: GET http://localhost:8734/panels.js returns 69770 bytes containing the exact new guard string -- fix is live in the served bundle. (2) exec_js-3.json: GET /api/prd?cwd=<unregistered-path> and /api/mutables?cwd=<unregistered-path> both return real HTTP 403 {"error":"cwd not in discovered project registry"} from the live server -- proves the error shape the new guard checks for is genuinely reachable in production, not hypothetical. (3) exec_js-5.json: GET / and /app.js both return HTTP 200 with non-trivial body length (661 and 19466 bytes) -- server healthy, no crash introduced by the edit. Every other Control-group panel (PrdEditor, MutablesEditor, RsTools, BrowserSessions, CodeInsightPanel, MemoryGraphPanel) already had this same error-Empty() guard; LifecycleControl was the sole omission -- audited via codesearch (ci-7b050b5e-e5ab8434 hits for each panel name) confirming all fetch every named panel's zero-data path reaches a real Empty(msg) rather than a blank/headers-only render, except this one bug now fixed.
