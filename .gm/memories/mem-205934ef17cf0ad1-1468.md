---
key: mem-205934ef17cf0ad1-1468
ns: default
created: 1785185993628
updated: 1785185993628
---

gmsniff session close-out: the PRD went 206 rows to 4, every closure witnessed by real execution rather than inspection, and the pattern across the whole session is that each remaining bug was a PLAUSIBLE WRONG NUMBER rather than a crash. Concrete instances found while closing rows: (1) parseYamlRows split on 
 so CRLF files -- which is how gm writes them on Windows -- parsed to ZERO rows, and gmsniff read its own 15-row prd.yml as empty with nothing thrown, an empty store looking exactly like a finished one; (2) ProjectLogTailer detected rotation by stat.size < offset so it recovered only when the replacement was strictly smaller, silently missing everything after an equal-or-larger rotation, fixed by comparing inode identity; (3) resolveScopedCwd rejected a live project 403 because discovery reads the filesystem while its cache only invalidated on events change, surfacing as an intermittent test failure which is worse than a consistent one; (4) rewriteRow re-emitted every row as - id: and yamlScalar quoted any value with a space, so editing one row reshaped gm own legacy subject/title cluster; (5) the publish workflow ran skip-check, bump and publish and NEVER ran the suite, so a red build published to npm unconditionally. Also settled: turn-state.json last_skill is not lagging the phase, it holds the NEXT phases skill (EXECUTE/gm-emit, VERIFY/gm-consolidate, COMPLETE/update-docs measured across four live projects) and was simply mislabelled.
