---
key: mem-0e119ddf8e6786f6-1514
ns: default
created: 1785199733927
updated: 1785199733927
---

gmsniff silent-truncation class, measured 2026-07-28: a list capped ONLY on the client cannot report what it omitted, because the response often carries no total to report it with -- and testing against gmsniff itself hides every instance. gui/panels.js sliced the memory graph to GRAPH_MAX_NODES=150 while /api/memory-graph returned {cwd, nodes, edges} with no count, so C:/dev/gm's 882 nodes rendered under a heading reading "150 nodes" with 732 gone. Fixed by paging at the ROUTE with total/returned/truncated (the contract /api/prd already uses) plus edges_total/edges_returned. Trap: readMemoryGraph emits edges as {src, dst, relation} and those only become {source, target} after the CLIENT maps them, so a server-side edge filter written against source/target silently drops EVERY edge. KEY MEASUREMENT: memory-file counts are gm 889, spoint 644, looper 159, gmsniff 63, aloop 17 -- gmsniff sits under the 150 cap, so these defects rendered identically to correct behavior when tested in this repo, which is why they survived. Witness every cap on a project that exceeds it AND one under it; a boundary emitting a bare "+0 more" is its own defect (looper at exactly the 8-verb cap correctly emits nothing). The run-metrics table capped at 8 while spoint exercises 20 distinct verbs, gm 19, gmsniff 16. Ellipsis arithmetic: slice(0, MAX - 1) plus a THREE-character ellipsis makes every truncated label MAX+2 wide -- 882 of 882 gm labels measured at 30 against a 28 limit; use slice(0, MAX - ELLIPSIS.length).
