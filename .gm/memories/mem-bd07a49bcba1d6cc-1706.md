---
key: mem-bd07a49bcba1d6cc-1706
ns: default
created: 1785196795656
updated: 1785196795656
---

gmsniff presentation-defect class, measured 2026-07-28: every defect found in the manager view was a CONFIDENT WRONG DISPLAY, never a crash, and each had its real data sitting unread one field away. (1) The health banner rendered watcherAlive -- which is readProjectLiveness().active, "is this PROJECT working" -- as the words "watcher not running", so four idle projects asserted a dead daemon while one shared agentplug-runner (pid 10648, status_ts age 23-197ms on all six projects) served every one of them; the same response already carried daemonPidAlive for the real question. The banner and the cards contradicted each other IN A SINGLE DOM READ (gm shown as both "watcher not running" and RUNNING), which is the signature of two independent derivations of one fact. (2) The event feed's detail chain (e.detail||e.reason||e.verb||e.phase||e.id||e.key||e.query) was assembled from memory and omitted `error` entirely, so embed_init_fail printed a bare name while its full text sat in that field -- measured 50.3% of 32919 real evt records rendering BLANK. (3) dispatch.end carries ms on 100% of 400 sampled records (median 560ms, max 339670ms = 5.7 minutes) and the feed threw it away. (4) limit=-5 reached slice() and took from the END of the array, returning the whole 10026-row store as 3.5MB. (5) /api/projects shipped a 23-field liveness sub-object worth 110KB of its 163KB that ZERO GUI surfaces read (verified by grep across all six gui modules). The through-line: check the producer's actual field names against the consumer's, measure coverage against RENDERED OUTPUT rather than the source data, and treat any two components rendering one fact as a drift risk until they share a derivation.
