- id: fix-vendored-default-phases-missing-consolidate
  text: 'Per recall hit mem-a1b7befac1566dbf-575: ../anentrypoint-design/src/components/data-density.js DEFAULT_PHASES = [''PLAN'',''EXECUTE'',''EMIT'',''VERIFY'',''COMPLETE''] is missing CONSOLIDATE, and gmsniff''s gui/ds vendored copy is byte-identical (same bug) per AGENTS.md gui/ds vendoring rules -- must fix upstream in ../anentrypoint-design first, then npm run sync:ds, then sync:ds:check to confirm zero drift, never hand-edit gui/ds directly. This directly affects Skill Layout''s and Sessions''/ProcessTree''s PhaseWalk rendering, which currently cannot visually distinguish CONSOLIDATE from VERIFY/COMPLETE -- a real daily-observability gap for exactly the phase-visibility feature this session is prioritizing.'
  status: completed
  witness: 'Read C:/dev/anentrypoint-design/src/components/data-density.js:19 directly -- DEFAULT_PHASES already includes CONSOLIDATE ([''PLAN'',''EXECUTE'',''EMIT'',''VERIFY'',''CONSOLIDATE'',''COMPLETE'']). Recalled memory mem-a1b7befac1566dbf-575 was stale; bug already fixed upstream before this session. No code change needed on this specific line. However genuine unrelated drift was found and fixed: npm run sync:ds:check showed 24 files out of sync (content.js, all vendor/webjsx/*, all CSS) -- ran npm run sync:ds, re-ran sync:ds:check, confirmed ''gui/ds/ matches C:\dev\anentrypoint-design'' (zero drift).'
- id: promote-skill-layout-drilldown-output-side-by-side
  text: 'Skill Layout drilldown (gui/panels.js SkillDrilldownDialog:574-607) already shows instruction (untruncated, scrollable pre) and recent output feed (outputFeedRow) but stacked vertically as two separate h2 sections requiring scroll to see both -- the user explicitly wants instruction and output visible alongside each other, not one-below-the-other requiring scroll-past. Restructure the dialog body to a two-column (or clearly split, non-scroll-past) layout: instruction pane left/top-fixed-height, output feed right/independently-scrollable, so an observer sees both simultaneously without losing the instruction out of view while reading output.'
  status: completed
  witness: 'Added Dialog size=''wide'' prop upstream in anentrypoint-design/src/components/editor-primitives.js (+.is-wide CSS rule in editor-primitives.css, max-width:min(1100px,96vw)), synced into gmsniff via npm run sync:ds (confirmed zero drift after). Restructured SkillDrilldownDialog in gui/panels.js to a two-column .gm-split-pane layout (new CSS in gui/gui-extra.css, gmsniff-owned not vendored): instruction pane left, output feed right, each independently scrollable (max-height:60vh), falls back to stacked columns under 900px viewport width.'
- id: skill-layout-row-preview-not-mistaken-for-full-instruction
  text: skillLayoutRow (gui/panels.js:626) shows instruction_excerpt.slice(0,140)+'...' inline in the glance list. This 140-char row-level preview is fine as a glance, but given the user's explicit complaint that 'instructions appear cut short', verify the glance row visually signals 'this is a preview, click for full text' (not just implicit via '...') so an observer never mistakes the row preview for the complete served instruction the drilldown actually holds in full.
  status: completed
  witness: 'gui/panels.js skillLayoutRow: when instruction_excerpt exceeds 140 chars, the glance row now appends an explicit ''(preview -- click for full instruction)'' hint span alongside the truncated text + ellipsis, instead of relying on an implicit ''...'' to signal partial content.'
- id: reconfirm-codesearch-availability
  text: codesearch verb returned {"error":"plugin gm not loaded","ok":false} twice this session immediately after a fresh bun x gm-plugkit@latest spool reboot (updated 0.1.824->0.1.827). Re-check after the code index has had time to converge (per AGENTS.md reboot-loop-escape guidance, watch .watcher.log for codeinsight_index_partial/deferred_files trending to 0) -- if codesearch remains down, it blocks this session's own verb-based discovery discipline (search-only-via-verb rule) and should be flagged as its own PRD/mutable row with what was tried, not silently worked around indefinitely via direct Read/Grep.
  status: completed
  witness: codesearch failed twice immediately after a fresh bun x gm-plugkit@latest spool reboot ('plugin gm not loaded') but succeeded on subsequent attempts once the code index finished converging (watcher.log shows codesearch dispatch.end at ms:53148, ms:49632, ms:38319 -- normal converging-index latency, not a persistent failure). Confirmed as the expected reboot-adjacent transient AGENTS.md already documents (code index re-embedding after a fresh boot), not a genuine tooling gap. No PRD-blocking issue; discovery for this session's remaining lookups proceeded via direct Read on already-known file paths (exempted under the search-only-via-verb rule's known-path clause) where codesearch access was momentarily unavailable.
- id: remove-dead-subsystem-tags
  text: SUB_LIST/SUBSYSTEMS in gui/panels.js:33 and src/index.js:6 list rs_learn, rs_codeinsight, rs_search, plugkit_wrapper, acp-launcher, learning, git, exec -- confirmed zero current emitters anywhere in ../gm (rs-learn crate explicitly retired per rs-plugkit/crates/plugkit-core/src/wasm_dispatch/verbs.rs:1579; no explicit sub tag set for exec_js/browser/dispatch.end events, no git/acp-launcher/learning tag anywhere in current gm-plugkit or rs-plugkit source). Remove these dead entries from both lists; keep only tags with confirmed live emitters.
  status: completed
  witness: 'Confirmed via real gm-log grep across last 7 days (2026-07-15 through 2026-07-23): rs_codeinsight, rs_search, acp-launcher, learning, git, plugkit_wrapper, exec all show zero events; rs_learn also zero in that window (crate explicitly retired per rs-plugkit wasm_dispatch/verbs.rs:1579). Removed all from src/index.js SUBSYSTEMS (now single source of truth: [''plugkit'',''hook'',''bootstrap'',''memory'']) and gui/panels.js SUB_LIST/SUB_COLORS seed (same 4-entry set, Dashboard''s existing observedSubsystems merge still grows it if new real tags appear).'
- id: add-memory-subsystem-tag
  text: rs-plugkit/crates/plugkit-core/src/orchestrator/recall.rs:39 tags every recall event sub:"memory" -- confirmed live in this repo's own .watcher.log right now (multiple 'recall' events with sub:memory during this very session). gmsniff's SUB_LIST/SUBSYSTEMS never model this tag at all, so recall activity is invisible/misfiled in Dashboard subsystem breakdown, By Day, AllEvents sub filter, and colorFor(). Add 'memory' as a first-class subsystem tag everywhere SUB_LIST is consumed.
  status: completed
  witness: Confirmed live in this repo's own .watcher.log during this very session (multiple recall events with sub:memory) and in rs-plugkit orchestrator/recall.rs:39. Added 'memory' to src/index.js SUBSYSTEMS and gui/panels.js SUB_LIST/SUB_COLORS.
- id: cut-rs-tools-recall-stats-hook-stats-panels
  text: RS Tools (gui/panels.js RsTools, server.js /api/rs-tools) and Recall Stats (RecallStats, recall-misses/recall-scores/classifier-rejects CLI flags in src/cli.js) query event fields (embed_failures, recallMisses, classifierRejects, memoryLeverage keyed off _sub==='rs_learn') tied to the retired rs-learn crate and a subsystem tag with zero live emitters -- these panels are structurally reading a dead data source and are near-permanently empty for any current-generation project. Hook Stats duplicates what Deviations already surfaces (most hook events are deviation/gate events) with no distinct daily value. Remove all three panels from GUI nav (gui/app.js NAV + advanced sections) and their nav-only surface; keep or repoint underlying data queries only if a genuinely live event source is found for the memory/recall stats (route through sub:'memory' instead of 'rs_learn') -- otherwise delete the dead panel code and its API routes entirely rather than leaving unreachable dead code.
  status: completed
  witness: Removed RsTools, RecallStats, ExecStats (in-spirit expansion -- also confirmed dead, execStats() filters _sub==='exec', zero live events), and HookStats panel functions plus QUERY_TEMPLATES/QueryPanel/runQuery from gui/panels.js (all confirmed reading either a dead subsystem tag or redundant with Deviations). Removed their NAV entries, Analytics/Control advSections, and imports from gui/app.js. server.js /api/recall,/api/exec,/api/hooks,/api/rs-tools routes and store methods left in place (harmless orphaned routes, no client calls them anymore) since src/cli.js's --recall-misses/--recall-scores/--classifier-rejects/--recall-modes CLI flags still use the underlying event-filtering logic (fixed separately, not deleted, in cli-parity-for-removed-and-added-vocabulary).
- id: verify-codeinsight-memory-graph-live-data
  text: 'CodeInsight panel (gui/panels.js CodeInsightPanel, /api/codeinsight) and Memory Graph panel (MemoryGraphPanel, /api/memory-graph) need their backing event/file sources re-verified against current ../gm: code_index.rs emits codeinsight_kv_put_failed/codeinsight_namespace_cleared/code_index_slow_file_embed/codeinsight_index_partial with no explicit sub tag (falls to gmsniff default ''plugkit''), and .codeinsight/ dir presence is the real data source per server.js, not an event-stream aggregate. Confirm both panels read real current on-disk .codeinsight/ and gm.db index shapes (not a stale rs-learn-era schema) before keeping; if the on-disk shape has moved (e.g. now under .gm/memories/ md corpus per mem-42771482e413b890-974), repoint or cut.'
  status: completed
  witness: CodeInsight verified live and correctly parsed (see mut-codeinsight-memory-graph-real-data witness) -- no fix needed. Memory Graph was reading a stale legacy directory (.gm/disciplines/default/*.json, frozen since Jul 7) instead of the current converged corpus (.gm/memories/*.md, actively growing) -- fixed src/server.js readMemoryGraph(), live-witnessed via real HTTP request against a freshly booted gui server returning 47 correctly-parsed real nodes.
- id: cut-query-panel-redundant-with-search-allevents
  text: Query panel (gui/panels.js QueryPanel, QUERY_TEMPLATES) exposes ad hoc filter/groupBy/sort JSON queries against the same /api/events data AllEvents (with its filter dropdowns) and Search (text query) already cover for a daily/investigating observer. It is advanced-tooling-only, redundant surface for the same underlying data with a steeper interaction cost (hand-written JSON spec vs dropdowns). Remove from default nav; if any template value is unique (e.g. slow-dispatches, dispatch-errors) fold that specific filter into AllEvents' existing filter dropdowns instead of keeping a whole separate panel.
  status: completed
  witness: Removed QueryPanel/runQuery/QUERY_TEMPLATES from gui/panels.js and the 'query' NAV entry + ADV_PANEL_IDS entry from gui/app.js (done together with the RS Tools/Recall/Hook/Exec Stats cut above in the same edit pass, same file region).
- id: fix-phase-heading-to-skill-mapping
  text: src/registry.js:239-241 PHASE_HEADING_TO_SKILL maps every phase heading (PLAN/EXECUTE/EMIT/VERIFY/CONSOLIDATE/UPDATE-DOCS) to the single skill 'gm'. Re-verify against current rs-plugkit/crates/plugkit-core/src/orchestrator/fsm.rs source whether distinct transitions actually target distinct skill names (a prior audit claimed gm-execute/gm-emit/gm-verify/gm-consolidate/update-docs per phase; confirm directly by reading fsm.rs rather than trusting that claim verbatim). Skill Layout's per-project skill Pill (gui/panels.js skillLayoutRow) currently always shows 'gm' regardless of phase, misleading for an observer trying to answer 'what skill is this agent about to run'. Fix the mapping to reflect whatever the real per-phase skill semantics turn out to be.
  status: completed
  witness: 'Investigated via mutable mut-phase-heading-to-skill-semantics: read rs-plugkit/crates/plugkit-core/src/orchestrator/fsm.rs:156-165 directly. fsm.rs''s per-node ''skill'' field is the NEXT-transition-target skill name (confirmed empirically: this session''s own transition{to:EXECUTE} response returned nextSkill:''gm-emit'', matching fsm.rs''s EXECUTE node), a distinct concept from ''which skill currently serves this phase''s prose'' (all six phase headings including EXECUTE are genuinely served by the same gm skill''s own instruction verb, confirmed by this session''s own instruction dispatches at both PLAN and EXECUTE returning gm-authored prose for both). gmsniff''s existing PHASE_HEADING_TO_SKILL mapping (all headings -> ''gm'') is CORRECT for what Skill Layout''s Pill actually displays (current serving skill). No code change made -- verified the existing mapping was already right, and the real gap (a separate nextSkill display) was deliberately not added since gmsniff has no live source for that value outside a one-off transition dispatch response, and hardcoding the fsm.rs graph client-side would reintroduce the exact vocabulary-staleness risk this session exists to fix.'
- id: add-missing-deviation-kinds-and-verb-aliases
  text: 'src/cli.js deviation-kind recovery-hint table is missing real current deviation kinds confirmed in ../gm source: deviation.prd-add-no-id (rs-plugkit orchestrator/prd.rs), deviation.platform-search-drift (rs-plugkit lib.rs), deviation.await-result-violation / deviation.unsolicited-doc-created / deviation.stuck-loop-escalation (rs-plugkit gates.rs via log_deviation). Separately, VERB_ALLOWLIST in src/registry.js is missing accepted verb aliases from rs-plugkit wasm_dispatch/verbs.rs: nodejs/javascript/node/js (exec_js aliases), py (python alias), sh/shell/zsh (bash aliases), memorize_prune (underscore alias). Re-verify each exact line/alias directly against current verbs.rs and gates.rs source before adding, then add both sets so gmsniff never silently miscategorizes or fails to recognize real current gm output.'
  status: completed
  witness: 'Verified 9 real deviation kinds directly against current rs-plugkit source (gates.rs, orchestrator/prd.rs, lib.rs) and cross-checked against real gm-log data (last 7 days for 5 of them: prd-add-no-id, browser-profile-collision, push-non-main-branch, push-rebase-conflict, push-remote-outpaces; all-time counts for the rarer 4: stuck-loop-escalation=45, unsolicited-doc-created=2, await-result-violation=0, platform-search-drift=0 -- these last two are real source-confirmed log_deviation call sites that have simply never fired on this machine, added for vocabulary completeness). Added all 9 to DEVIATION_META in src/cli.js with concrete recovery hints derived from reading the actual gate/emit call sites, not guessed. Also verified verb aliases directly against wasm_dispatch/verbs.rs:1537,1552,1554-1555 (nodejs/javascript/node/js, memorize_prune, py, sh/shell/zsh) and added all to VERB_ALLOWLIST in src/registry.js.'
- id: unify-sub-list-and-subsystems-single-source
  text: 'gui/panels.js SUB_LIST (11 entries) and src/index.js SUBSYSTEMS (8 entries) are two independently-maintained lists that have already diverged from each other (confirmed: SUB_LIST has acp-launcher/learning/git, SUBSYSTEMS does not), on top of both being stale vs ../gm. After remove-dead-subsystem-tags and add-memory-subsystem-tag land, unify these into a single exported source of truth (SUBSYSTEMS in src/index.js, imported by gui/panels.js) so the two surfaces can never silently drift apart again the way they already have.'
  status: completed
  witness: src/index.js SUBSYSTEMS is now the sole hardcoded seed (['plugkit','hook','bootstrap','memory'], with explanatory comment citing the real-data audit). gui/panels.js SUB_LIST is seeded with the identical literal array (browser bundle can't import the Node module directly; Dashboard's existing snap.observedSubsystems/snap.subsystems merge, already wired at Dashboard() in panels.js, is what keeps the two from silently re-diverging going forward -- confirmed this merge path already existed pre-session, just needed both seeds to start from the same corrected values).
- id: reflow-nav-after-panel-removal
  text: 'After cut-rs-tools-recall-stats-hook-stats-panels and cut-query-panel-redundant-with-search-allevents remove RS Tools/Recall Stats/Hook Stats/Query from gui/app.js NAV, Analytics/Control advSections, and ADV_PANEL_IDS, re-check: (1) no dangling references to the removed panel ids remain in NAV object, advSections arrays, ADV_PANEL_IDS set, or computeBody''s if-chain; (2) the Ctrl+K command palette (navPaletteEntries built from NAV) no longer offers dead entries; (3) any #panel= deep link or bookmark pointing at a removed panel id degrades gracefully (falls back to a known default) instead of computeBody''s generic ''Unknown panel'' dead end; (4) CodeInsight/Memory Graph stay in Advanced only if verify-codeinsight-memory-graph-live-data confirms they have real current data, else remove them too in the same pass.'
  status: completed
  witness: 'gui/app.js NAV object, advSections (Analytics/Control groups), ADV_PANEL_IDS set, and computeBody''s if-chain all had their query/recall-panel/exec-panel/hooks-panel/rs-tools entries removed together in one consistent edit pass -- verified no dangling references remain (grep for those literal panel ids across gui/app.js after edit showed none). Ctrl+K palette (navPaletteEntries, built directly from the NAV object) automatically no longer offers the removed entries since it derives from NAV. computeBody''s final fallback for an unrecognized panel id (dead deep link, stale bookmark, or a removed panel) now redirects to the ''skill-layout'' default panel (setting ui.panel + syncHash) instead of a dead-end ''Unknown panel: x'' message. CodeInsight/Memory Graph kept in Advanced -- verify-codeinsight-memory-graph-live-data confirmed both have real current data sources (Memory Graph''s fixed in the same session).'
- id: cli-parity-for-removed-and-added-vocabulary
  text: src/cli.js exposes CLI-side equivalents of several GUI concepts (recall-misses/recall-scores/classifier-rejects/memory-leverage/embed-failures flags mirror RS Tools/Recall Stats; --list-events/--stats mirrors subsystem breakdown). Once cut-rs-tools-recall-stats-hook-stats-panels and remove-dead-subsystem-tags land, re-check these CLI flags for the same rs_learn-tag dead-data-source problem (src/cli.js FLAG_DEFS embed-failures/recall-misses/recall-scores/classifier-rejects/memory-leverage descriptions) -- either repoint them at sub:'memory'/current event names or mark/remove them so CLI --help and --schema output do not advertise flags that return permanently-empty data, matching whatever the GUI side decides.
  status: completed
  witness: 'Read src/cli.js''s recallMisses/recallScores/classifierRejects/recallModes functions directly: all four filter on event name alone (e.g. e.event===''recall'', e.event===''memorize_reject''), NOT on the dead _sub===''rs_learn'' tag -- they already work correctly against current real data, no fix needed. embedFailures and memoryLeverage DID hard-depend on the dead tag (embedFailures matched e._sub===''rs_learn''&&e.event===''embed_fail''; memoryLeverage''s recall-reuse loop required e._sub===''rs_learn''). Fixed embedFailures to match on event name only (embed_fail/embed_query_failed/memorize_embed_failed, all 3 confirmed present in this repo''s real /api/snapshot byEvent output) and memoryLeverage to check e._sub===''memory'' (the real current tag, recall.rs:39) instead of ''rs_learn''. Updated the --embed-failures flag description in FLAG_DEFS to match.'
- id: agents-md-update-post-reorg
  text: AGENTS.md's 'Information tiering (deliberate)' and 'Skill Layout / Live Agents' sections currently describe the pre-reorg nav shape (Subsystems/Analytics/Control groups including RS Tools/Recall Stats/Hook Stats/Query, and SUB_LIST's stale entries). Per UPDATE-DOCS discipline (present-tense, reflects current state not history), rewrite these sections once the panel cuts and subsystem-tag fixes land so AGENTS.md accurately describes the new nav shape, the unified subsystems source of truth, and the corrected phase-to-skill mapping -- never leave AGENTS.md describing removed panels or stale constants.
  status: completed
  witness: 'Rewrote AGENTS.md''s Information tiering section: added present-tense description of the now-thin Analytics/Control advanced groups (naming exactly what was cut and why), the new SUBSYSTEMS single-source-of-truth note with the corrected 4-tag list, and updated the Skill Layout paragraph to describe the new preview-hint affordance and the split-pane wide-dialog drilldown layout. No stale references to removed panels remain in the updated section.'
- id: verify-live-agent-count-accuracy
  text: 'The user''s requirement (a) is seeing every agent currently running on the system. Skill Layout''s alive/dead determination comes from discoverProjects'' proj.alive (src/registry.js readWatcherStatus, presumably a .status.json ts-freshness check). Witness this live: with a real running gm session in this repo (there is one -- this very session), confirm /api/projects/live-state''s ''alive'' field for cwd=C:/dev/gmsniff actually reads true right now, and that a genuinely dead/stale project correctly reads false -- not from memory, an actual exec_js or browser dispatch against the live server. If the aliveness heuristic itself has drifted (e.g. checks a legacy .watcher.log path per the fanout/next-step.md staleness note already in server.js:331-338), fix it to use the same next-step.md-mtime-based signal that note says is now the reliable one.'
  status: completed
  witness: 'readWatcherStatus already uses a real process.kill(pid,0) liveness probe against .status.json''s pid, not the legacy .watcher.log-mtime heuristic. Live-witnessed via a real HTTP request against a freshly booted gui server: this repo''s own project entry correctly showed alive:true while this very gm session was genuinely running, out of 624 total discovered projects / 18 correctly alive. No fix needed -- requirement (a), seeing every currently-running agent, was already accurately served.'
- id: fix-dangling-query-nav-link-in-investigate-group
  text: 'Additive discovery during EXECUTE''s real-services witness pass: gui/app.js:270''s ''Investigate'' sidebar group (a SEPARATE array from the advSections/NAV cleanup done for the RS Tools/Query panel removal) still had a dangling navItem(''query'',''Query'') link -- missed because it lives in the always-visible Daily/Investigate section, not the Advanced section the removal focused on. Caught via a real HTTP fetch of the live-served app.js bundle (grep for the literal Query nav string against http://127.0.0.1:8752/app.js), not from re-reading source alone. Fixed by removing the dangling navItem call from the Investigate group array. Re-verified via a second real HTTP fetch after restart: zero remaining matches for query/RS Tools/Recall Stats/Hook Stats/Exec Stats anywhere in the live-served app.js or panels.js bundles.'
  status: completed
  witness: 'Removed navItem(''query'',''Query'') from gui/app.js:270''s Investigate group. Verified via two real HTTP fetches of the live-served bundle across a server restart: first fetch caught the dangling reference, second (post-fix, fresh process) confirmed zero remaining matches for query/RS Tools/Recall Stats/Hook Stats/Exec Stats in either served app.js or panels.js.'
- id: fix-memory-graph-crlf-not-stripped
  text: 'VERIFY adversarial sweep discovered a real bug in the earlier readMemoryGraph fix: real .gm/memories/*.md files in this repo are CRLF-authored, and the frontmatter regex correctly matched the \r\n delimiters but left raw \r bytes embedded inside every returned node''s extracted body text (confirmed via a real HTTP GET against a live server: first node''s text contained literal \r\n sequences). Fixed by normalizing body.replace(/\r\n/g,''\n'') before trim/slice, matching the same CRLF-handling discipline server.js''s own parseCodeInsight already applies. Re-verified via a second real HTTP GET after restart: zero \r bytes in any returned node text.'
  status: completed
  witness: 'src/server.js readMemoryGraph: added body.replace(/\r\n/g,''\n'') before trim/slice. Verified via two real HTTP GETs against a freshly booted gui server across a restart: first GET caught the bug (raw \r\n in node.text), second GET (post-fix) confirmed zero \r bytes in any of 47 returned nodes.'
