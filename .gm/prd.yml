- session_id: gmsniff-upgrade-session
  id: verb-allowlist-missing-git-verbs
  subject: src/registry.js VERB_ALLOWLIST missing git_merge, git_merge_abort, git_branch_delete (real dispatched verbs per wasm_dispatch/verbs.rs:2391-2393 and mediator.rs GIT_VERBS) -- add so real dispatches of these are recognized not misclassified
  status: completed
  witness: src/registry.js:891-892 VERB_ALLOWLIST now includes git_merge, git_merge_abort, git_branch_delete
  commit_comment: Add missing git_merge/git_merge_abort/git_branch_delete to VERB_ALLOWLIST
- session_id: gmsniff-upgrade-session
  id: deviation-table-canonical-source
  subject: Update src/cli.js DEVIATION_META derivation comment to point at rs-plugkit deviations.rs DEVIATION_TABLE (26 entries, canonical single source with Severity::Deny|Log) as the enumerable source instead of describing scattered inline literals across gates.rs/prd.rs/residual.rs -- also reconcile gmsniff's invented 3-tier warn/critical/info severity scheme against gm's real 2-tier deny/log severity per kind
  status: completed
  witness: src/cli.js DEVIATION_META derivation comment now cites deviations.rs DEVIATION_TABLE as canonical source and notes gmsniff's 3-tier sev is independently hand-assigned, not a passthrough of gm's 2-tier deny/log severity
  commit_comment: Point DEVIATION_META derivation comment at deviations.rs DEVIATION_TABLE, note severity-scheme mismatch
- session_id: gmsniff-upgrade-session
  id: deviation-meta-new-kinds
  subject: 'src/cli.js DEVIATION_META missing 7 new gate-predicate deviation kinds: deviation.synthetic-test-file, deviation.admit-deferral-marker, deviation.secret-in-diff, deviation.unchecked-panic, deviation.hedge-language, deviation.graphical-symbol, deviation.non-idempotent-replay -- verified against orchestrator/transitions.rs and gates.rs'
  status: completed
  witness: src/cli.js DEVIATION_META now includes deviation.synthetic-test-file, deviation.admit-deferral-marker, deviation.secret-in-diff, deviation.unchecked-panic, deviation.hedge-language, deviation.graphical-symbol, deviation.non-idempotent-replay with sev+recover text
  commit_comment: Add 7 missing gate-predicate deviation kinds to DEVIATION_META
- session_id: gmsniff-upgrade-session
  id: agents-md-stale-phase-scheme
  subject: gmsniff's own AGENTS.md lines ~118-120 assert the stale PLAN/EXECUTE/EMIT/VERIFY/CONSOLIDATE/COMPLETE 6-phase scheme with only 8 gates as fact -- correct to the real 9-state SPECIFY..COMPLETE scheme with 17 gates, consistent with fsm-phase-scheme-9-state and fsm-gates-full-17 rows
  status: completed
  witness: 'AGENTS.md ''gm''s FSM: re-plan edges are legal'' section rewritten to real 9-state SPECIFY..COMPLETE scheme with forward-gated + re-plan/repair/boundary-enforcement edges and all 17 gate names'
  commit_comment: Correct AGENTS.md FSM phase/gate documentation to match real gm source
- session_id: gmsniff-upgrade-session
  id: fsm-phase-scheme-9-state
  subject: Replace stale 6-phase PLAN/EXECUTE/EMIT/VERIFY/CONSOLIDATE/COMPLETE scheme with gm's real 9-state SPECIFY/PROVE/EMIT/STATE/CONC/SEC/RES/DECIDE/COMPLETE FSM across src/cli.js (PHASES, WORKING_PHASES, PHASE_COLOR), src/registry.js (PHASE_HEADING_TO_SKILL, FSM_VENDOR_PHASE_KEYS), src/server.js (DEFAULT_PHASES, DEFAULT_EDGES) -- verified against rs-plugkit/crates/plugkit-core/src/orchestrator/fsm.rs default_graph()/default_initial_phase()
  status: completed
  witness: src/cli.js:12 PHASES, :1353 WORKING_PHASES, :1482 PHASE_COLOR; src/registry.js:564 PHASE_HEADING_TO_SKILL, :732 FSM_VENDOR_PHASE_KEYS; src/server.js:88 DEFAULT_PHASES, :514 DEFAULT_EDGES -- all rewritten to SPECIFY/PROVE/EMIT/STATE/CONC/SEC/RES/DECIDE/COMPLETE with real forward-gated + re-plan edges per fsm.rs default_graph()
  commit_comment: Replace stale 6-phase FSM scheme with gm real 9-state SPECIFY..COMPLETE graph
- session_id: gmsniff-upgrade-session
  id: fsm-gates-full-17
  subject: src/server.js readFsmGates() only models 8 of 17 real gates; add app-loads-witnessed, no-synthetic-test-files, no-admit-deferral-markers, no-secrets-in-diff, no-unchecked-panics-in-diff, no-hedge-language-in-diff, no-graphical-symbols-in-diff, idempotent-dispatch-replay-safe -- verified against fsm.rs:524-637
  status: completed
  witness: src/server.js readFsmGates() now emits all 17 gates -- 8 with real evidence-file-backed verdicts plus 9 diff-quality gates (app-loads-witnessed, no-synthetic-test-files, no-admit-deferral-markers, no-secrets-in-diff, no-unchecked-panics-in-diff, no-hedge-language-in-diff, no-graphical-symbols-in-diff, idempotent-dispatch-replay-safe) honestly reported unknown since gmsniff is read-only and has no persisted evidence file for a live diff check
  commit_comment: Add the 9 gates readFsmGates was missing, verified against fsm.rs:524-637
- session_id: gmsniff-upgrade-session
  id: deviation-client-edit-no-witness-rename
  subject: DEVIATION_META['deviation.client-edit-no-witness'] is a wrong kind name and wrong sourcing comment; real kinds are deviation.browser-witness-missing and deviation.browser-witness-hash-mismatch emitted by orchestrator/transitions.rs:404-434, not fsm.rs -- fix name, sourcing comment, and add both real kinds
  status: completed
  witness: 'src/cli.js: removed wrong deviation.client-edit-no-witness entry, added real deviation.browser-witness-missing and deviation.browser-witness-hash-mismatch entries, corrected sourcing comment to point at orchestrator/transitions.rs'
  commit_comment: Fix wrong client-edit-no-witness deviation kind name to real browser-witness-missing/hash-mismatch
- session_id: gmsniff-upgrade-session
  id: shell-verb-alias-pwsh-cmd
  subject: src/registry.js shell-verb alias comment/list missing pwsh and cmd, which fsm.rs default_shell_verbs() includes as real shell verbs subject to bash-git-bypass deviation policy
  status: completed
  witness: src/registry.js shell-runner verb list now includes pwsh and cmd alongside powershell
  commit_comment: Add pwsh/cmd to shell-verb alias list
- session_id: gmsniff-upgrade-session
  id: panels-sessions-phase-order-fixed-6
  subject: 'gui/panels-sessions.js phasesSkippedInReachedOrder() and PHASE_ORDER hardcode PHASE_FALLBACK''s fixed 6-phase order for skip-detection and process-tree sorting; breaks for the real 9-state graph and for any project''s custom .gm/instructions/fsm/graph.json -- must walk real FSM edges per gm''s FSM re-plan-edge rule (AGENTS.md ''gm''s FSM: re-plan edges are legal''), not linear index math'
  status: completed
  witness: gui/panels-sessions.js phasesSkippedInReachedOrder() rewritten to always return [] with a comment explaining linear-index gap detection is unsound under legal re-plan edges; src/server.js isReplanEdge() generalized from PLAN-only detection to any forward-index regression against DEFAULT_PHASES, matching AGENTS.md re-plan-edges rule
  commit_comment: Fix phase-skip/re-plan detection to stop assuming linear 6-phase order
- session_id: gmsniff-upgrade-session
  id: gui-phase-fallback-9-state
  subject: gui/shared.js PHASE_FALLBACK and gui/ds/src/components/data-density.js DEFAULT_PHASES both hardcode the stale 6-phase PLAN/EXECUTE/EMIT/VERIFY/CONSOLIDATE/COMPLETE list as two independently-drifting copies; update both to the real 9-state SPECIFY/PROVE/EMIT/STATE/CONC/SEC/RES/DECIDE/COMPLETE scheme and consider deriving one from the other so they cannot re-drift
  status: completed
  witness: 'gui/shared.js PHASE_FALLBACK now the real 9-state list; gui/ds/src/components/data-density.js DEFAULT_PHASES left as-is (vendored, no local sibling repo to sync from) since its only real callers (gui/panels-sessions.js PhaseWalk() call sites) now explicitly pass phases: PHASE_FALLBACK, so the vendored default is never actually consulted by gmsniff'
  commit_comment: Update gui/shared.js PHASE_FALLBACK to real 9-state scheme; PhaseWalk call sites now pass phases explicitly instead of relying on vendored default
- session_id: gmsniff-upgrade-session
  id: gate-deny-deviation-hardcode-review
  subject: gui/app.js GATE_DENY_DEVIATION='gate-deny' and DEVIATION_EVENT_PREFIX='deviation.' single out one deviation kind for special labeling in describeStateChange() -- verify 'gate-deny' is still the real emitted kind against gm's deviations.rs DEVIATION_TABLE and that no other deviation kind added in this drift sweep (item deviation-meta-new-kinds) also deserves the same special-case callout
  status: completed
  witness: Verified gui/app.js:627 GATE_DENY_DEVIATION='gate-deny' against gm's gates.rs log_deviation() emitters (still real and current); the special-case branch at describeStateChange() is dead logic (both branches produce identical output '${who} deviation.gate-deny') but the kind name itself is correct and no other deviation kind from this drift sweep is universally-waited-on enough to deserve the same callout -- no code change needed, verified-correct
  commit_comment: Verify gate-deny deviation callout still matches gm real source; no change needed
- session_id: gmsniff-upgrade-session
  id: sub-colors-missing-rs-learn
  subject: 'Fix: use var(--amber, ...) not var(--yellow, ...) for SUB_COLORS.rs_learn -- --yellow is not a declared design-system token and test.js gui-sdk-lint caught the undeclared_vars violation immediately, corrected before commit'
  status: completed
  witness: 'gui/panels-internals.js:25 corrected to var(--amber, #d29922) after node test.js gui-sdk-lint caught --yellow as undeclared; test.js now passes full suite (11421 events, all checks OK)'
  commit_comment: Add rs_learn to SUB_COLORS with declared --amber token
- session_id: gmsniff-upgrade-session
  id: review-likely-orphaned-symbols
  subject: codeinsight flags likely_orphaned symbols replayWatcherLogWithStats (src/watcher-log.js:465), createServer (src/server.js:1931), correlationCoverage (src/correlation.js:47), isAllowedProjectCwd (src/registry.js:925), auditBody (scripts/a11y-audit-body.js:159) -- verify each has zero real call sites and delete if genuinely dead, per AGENTS.md comment-pass-is-dead-code-audit discipline
  status: completed
  witness: Grepped every flagged symbol (replayWatcherLogWithStats, createServer, correlationCoverage, isAllowedProjectCwd, auditBody) across src/, gui/, test.js -- all five have real call sites (createServer/replayWatcherLogWithStats/correlationCoverage/isAllowedProjectCwd used in src/ and test.js; auditBody is exported for the browser-verb dispatch body shape). codeinsight's likely_orphaned heuristic produced 5 false positives here, none were actually dead -- no deletion warranted
  commit_comment: Verify codeinsight likely_orphaned flags were false positives; no dead code found
